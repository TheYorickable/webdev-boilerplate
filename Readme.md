# Requirements
- Docker
- NPM
- Gulp installed globally ( `npm i -g gulp` )

# Setup
Install dependencies
`npm i`

Start docker container
`docker-compose up -d`

Build assets
`npm run dev`

Open browser
`open http://localhost`
